name: rotten_chickens
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  carousel_slider: ^4.0.0
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.0
  fluttertoast: ^8.0.8
  keyboard_actions: ^3.4.4
  lottie: ^1.2.1
  shared_preferences: ^2.0.8

dev_dependencies:
  build_runner: ^2.1.2
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  freezed: ^0.14.5

flutter:
  uses-material-design: true

  assets:
  
    - assets/
